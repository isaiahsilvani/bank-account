export enum ActionType {
  DEPOSIT = "deposit",
  WITHDRAW = "withdraw",
  BANKRUPT = "bankrupt",
  MULTIPLY = "multiply",
  SET = 'set'
}

// enum has to be one of the specific things we put inside enum